apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: heimdall-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/heimdall/heimdall-helm-values
  type: Opaque