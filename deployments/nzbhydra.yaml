---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: nzbhydra
  namespace: default
  annotations:
    release: "nzbhydra"
    flux.weave.works/automated: "true"
    flux.weave.works/tag.chart-image: "glob:amd64-v2.*.*-*"
spec:
  releaseName: nzbhydra
  chart:
    git: git@ssh.dev.azure.com:v3/ryholt/HomeAutomation/k8s-helm
    path: charts/nzbhydra
    ref: master
  values:
    image:
      repository: linuxserver/hydra2
      tag: amd64-v2.6.11-ls24
    service:
      type: ClusterIP
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "traefik"
    timezone: "America/New_York"
    persistence:
      downloads:
        enabled: true
        existingClaim: tank-docker-pvc
        subPath: nzbget/downloads
      config:
        enabled: true
        storageClass: "fast-disks"
        size: 30Gi
  valueFileSecrets:
  - name: "nzbhydra-values"
