---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
postRenderers:
  - kustomize:
      patchesJson6902:
        - target:
            kind: Deployments
            name: mailu-front
          patch:
            - op: add
              path: /spec/template/spec/hostname
              value: mailu-front
