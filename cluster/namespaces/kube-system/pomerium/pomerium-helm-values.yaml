---
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: pomerium-helm-values
  namespace: kube-system
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: pomerium-helm-values
      name: values.yaml
