apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: democratic-csi-helm-values
  namespace: kube-system
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: democratic-csi-helm-values
      name: values.yaml
