apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: dex-k8s-authenticator-helm-values
  namespace: default
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: dex-k8s-authenticator-helm-values
      name: values.yaml
      property: value
