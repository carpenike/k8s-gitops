config:
  sharedSecret: $POMERIUM_SHARED_SECRET
  cookieSecret: $POMERIUM_COOKIE_SECRET
extraEnv:
  IDP_SCOPES: "openid profile email offline_access groups"
authenticate:
  idp:
    provider: oidc
    url: "https://dex.holthome.net"
    clientID: pomerium
    clientSecret: $DEX_POMERIUM_CLIENT_SECRET
