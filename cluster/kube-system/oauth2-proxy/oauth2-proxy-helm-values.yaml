apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: oauth2-proxy-helm-values
  namespace: kube-system
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: oauth2-proxy-helm-values
      name: values.yaml
      property: value
