---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: esphome
  namespace: default
spec:
  releaseName: esphome
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: esphome
      version: 4.3.1
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: esphome/esphome
      tag: 1.15.3
    env:
      TZ: "America/New_York"
      ESPHOME_DASHBOARD_USE_PING: true
    hostNetwork: true
    dnsPolicy: ClusterFirstWithHostNet
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "nginx"
        cert-manager.io/cluster-issuer: letsencrypt-prod
        kubernetes.io/tls-acme: "true"
        ingress.pomerium.io/allowed_groups: '["k8s-admins"]'
        nginx.ingress.kubernetes.io/auth-url: "https://forwardauth.holthome.net/verify?uri=$scheme://$host$request_uri"
        nginx.ingress.kubernetes.io/auth-signin: "https://forwardauth.holthome.net/?uri=$scheme://$host$request_uri"
      hosts:
      - host: esphome.holthome.net
        paths:
        - path: /
          pathType: Prefix
      tls:
      - hosts:
        - esphome.holthome.net
        secretName: esphome-cert
    persistence:
      config:
        enabled: true
        existingClaim: esphome-pvc
