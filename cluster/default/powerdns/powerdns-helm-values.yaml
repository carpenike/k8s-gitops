apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: powerdns-helm-values
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: powerdns-helm-values
      name: values.yaml
      property: value
