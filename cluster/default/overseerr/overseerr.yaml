---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: overseerr
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: overseerr
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
      interval: 5m
  values:
    controllerType: deployment
    image:
      repository: sctx/overseerr
      tag: 1.18.0
      pullPolicy: Always
    env:
      TZ: "America/New_York"
      LOG_LEVEL: "info"
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "nginx"
        kubernetes.io/tls-acme: "true"
        cert-manager.io/cluster-issuer: letsencrypt-prod
      hosts:
      - host: overseerr.holthome.net
        paths:
        - path: /
          pathType: Prefix
      tls:
      - hosts:
        - "overseerr.holthome.net"
        secretName: overseerr-cert
    podAnnotations:
      backup.velero.io/backup-volumes: config
    persistence:
      config:
        enabled: true
        mountPath: /app/config
        existingClaim: overseerr-pvc
      requests:
        memory: 250Mi
        cpu: 500m
      limits:
        memory: 1500Mi
