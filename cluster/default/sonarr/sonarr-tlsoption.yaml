apiVersion: traefik.containo.us/v1alpha1
kind: TLSOption
metadata:
  name: sonarr-tlsoption
  namespace: default
spec:
  clientAuth:
    secretNames:
      - bigcerts-ca-cert
    clientAuthType: RequireAndVerifyClientCert
