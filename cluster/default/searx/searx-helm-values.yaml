apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: searx-helm-values
  namespace: default
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: searx-helm-values
      name: values.yaml
      property: value
