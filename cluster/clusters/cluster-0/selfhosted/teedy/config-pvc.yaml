---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teedy-config-pvc
  namespace: selfhosted
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
