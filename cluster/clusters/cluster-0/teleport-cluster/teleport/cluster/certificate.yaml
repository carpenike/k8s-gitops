---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "teleport.${SECRET_DOMAIN/./-}"
  namespace: teleport-cluster
spec:
  secretName: "teleport.tls"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "teleport.${SECRET_DOMAIN}"
