---
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: velero-helm-values
  namespace: velero
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: velero-helm-values
      name: values.yaml
