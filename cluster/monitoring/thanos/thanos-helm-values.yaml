---
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: thanos-helm-values
  namespace: monitoring
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: thanos-helm-values
      name: values.yaml
