---
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: hydra-helm-values
  namespace: security
spec:
  backendType: azureKeyVault
  keyVaultName: holthome
  data:
    - key: hydra-helm-values
      name: values.yaml
