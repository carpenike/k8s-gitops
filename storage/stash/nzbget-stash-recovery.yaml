apiVersion: stash.appscode.com/v1alpha1
kind: Recovery
metadata:
  name: stash-nzbget
  namespace: default
spec:
  repository:
    name: deployment.nzbget
    namespace: default
  snapshot: deployment.nzbget-aaf20245
  backend:
    azure:
      container: backups
      prefix: stash
    storageSecretName: azure-secret
  paths:
    - /config
  recoveredVolumes:
    - mountPath: /config
      persistentVolumeClaim:
        claimName: nzbget-config
