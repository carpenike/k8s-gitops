apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: bookstack-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/bookstack/bookstack-helm-values
  type: Opaque
