apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: drone-helm-values
  namespace: drone
spec:
  keys:
    - values.yaml
  path: secrets/drone/drone/drone-helm-values
  type: Opaque
