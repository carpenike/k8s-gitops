apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: consul-helm-values
  namespace: kube-system
spec:
  keys:
    - values.yaml
  path: secrets/kube-system/consul/consul-helm-values
  type: Opaque
