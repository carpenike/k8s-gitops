---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qb-config-pvc
  namespace: media
  labels:
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block
