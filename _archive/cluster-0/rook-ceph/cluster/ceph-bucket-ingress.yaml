---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ceph-bucket
  namespace: rook-ceph
  annotations:
    external-dns.alpha.kubernetes.io/target: "ipv4.holthome.net"
spec:
  ingressClassName: nginx
  rules:
    - host: "s3.holthome.net"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rook-ceph-rgw-ceph-objectstore
                port:
                  number: 80
  tls:
    - hosts:
        - "s3.holthome.net"
