---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-0-config-pvc
  namespace: network
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
