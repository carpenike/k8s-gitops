---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: changedetection-io-config-pvc
  namespace: tools
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-ceph-block
