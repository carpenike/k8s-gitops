---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qb-config-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block
