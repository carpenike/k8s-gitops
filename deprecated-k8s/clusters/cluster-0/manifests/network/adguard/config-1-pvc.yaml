---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-1-config-pvc
  namespace: network
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
