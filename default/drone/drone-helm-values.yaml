apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: drone-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/drone/drone-helm-values
  type: Opaque