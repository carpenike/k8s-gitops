apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: pgadmin-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/home-assistant/pgadmin-helm-values
  type: Opaque
