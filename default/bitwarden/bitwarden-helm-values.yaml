apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: bitwarden-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/bitwarden/bitwarden-helm-values
  type: Opaque